_schema-version: "3.2"
ID: compracticenorthwind
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: compracticenorthwind-destination-content
  type: com.sap.application.content
  requires:
  - name: compracticenorthwind-destination-service
    parameters:
      content-target: true
  - name: compracticenorthwind-repo-host
    parameters:
      service-key:
        name: compracticenorthwind-repo-host-key
  - name: compracticenorthwind-uaa
    parameters:
      service-key:
        name: compracticenorthwind-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: compracticenorthwind_html_repo_host
          ServiceInstanceName: compracticenorthwind-html5-srv
          ServiceKeyName: compracticenorthwind-repo-host-key
          sap.cloud.service: compracticenorthwind
        - Authentication: OAuth2UserTokenExchange
          Name: compracticenorthwind_uaa
          ServiceInstanceName: compracticenorthwind-xsuaa-srv
          ServiceKeyName: compracticenorthwind-uaa-key
          sap.cloud.service: compracticenorthwind
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: compracticenorthwind-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: compracticenorthwind-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - compracticenorthwind.zip
      name: compracticenorthwind
      target-path: resources/
- name: compracticenorthwind
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: compracticenorthwind-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: compracticenorthwind-destination-service
    service-plan: lite
- name: compracticenorthwind-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: compracticenorthwind-xsuaa-srv
    service-plan: application
- name: compracticenorthwind-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: compracticenorthwind-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
